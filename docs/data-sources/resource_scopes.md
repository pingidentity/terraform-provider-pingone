---
page_title: "pingone_resource_scopes Data Source - terraform-provider-pingone"
subcategory: "SSO"
description: |-
  Data source to retrieve the IDs of all OAuth 2.0 resource scopes for a specified resource in a PingOne environment.
---

# pingone_resource_scopes (Data Source)

Data source to retrieve the IDs of all OAuth 2.0 resource scopes for a specified resource in a PingOne environment.

## Example Usage

```terraform
data "pingone_resource_scopes" "custom_resource_scopes" {
  environment_id = var.environment_id
  resource_id    = pingone_resource.my_custom_resource.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `environment_id` (String) The ID of the environment that contains the resource.  Must be a valid PingOne resource ID.  This field is immutable and will trigger a replace plan if changed.
- `resource_id` (String) The ID of the resource to retrieve scopes from.  Can be a custom resource, or a built-in resource such as `openid` or `PingOne API`.

### Read-Only

- `ids` (Set of String) A set of IDs of all resource scopes that belong to the specified resource.  The IDs can be used with the `pingone_resource_scope` data source to retrieve full scope details.
