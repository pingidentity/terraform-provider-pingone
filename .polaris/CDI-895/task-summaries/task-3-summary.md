# Task 3 Summary: Regenerate provider documentation

## What Was Implemented

Ran `make generate` to invoke `tfplugindocs` and regenerate all docs, including `docs/resources/application.md` and `docs/data-sources/application.md`. No files in `docs/` were manually edited. The regenerated docs now reflect the updated CORS origins limit of 40 in all previously stale locations.

`make generate` internally runs `build` (go build) and `fmt` (terrafmt + terraform fmt) before calling `go tool tfplugindocs generate --provider-name terraform-provider-pingone`. All steps completed without error.

## Files Modified

- `docs/resources/application.md` — regenerated by tfplugindocs; "Limited to 20 values." replaced by "Limited to 40 values." in all three `origins` attribute descriptions (previously at lines 401, 503, 593)
- `docs/data-sources/application.md` — regenerated by tfplugindocs; "Limited to 20 values." replaced by "Limited to 40 values." in all three `origins` attribute descriptions (previously at lines 134, 215, 293)

## Verification

- Tests: not run (make generate does not run tests; no test changes in this task)
- Lint: skipped (not part of make generate; separate lint toolchain)
- Typecheck: passed implicitly — `make generate` invokes `go build` as a prerequisite and it succeeded
- Build: passed — `go build` (via `make generate` → `build` target) completed with exit code 0
- Acceptance criteria checks:
  - `grep "Limited to 20 values." docs/resources/application.md` — no matches (PASS)
  - `grep "Limited to 40 values." docs/resources/application.md` — 3 matches (PASS)
  - `grep "Limited to 20 values." docs/data-sources/application.md` — no matches (PASS)
  - `grep "Limited to 40 values." docs/data-sources/application.md` — 3 matches (PASS)

## Deviations

None.
