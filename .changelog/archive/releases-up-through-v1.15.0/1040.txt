```release-note:bug
`resource/pingone_gateway`: Resolved `inconsistent result after apply` error when `description` is supplied for `RADIUS` and `LDAP` types.
```

```release-note:bug
`resource/pingone_gateway`: Corrected the type of `radius_davinci_policy_id`, resulting in format validation that no longer incorrectly enforces dash characters.
```