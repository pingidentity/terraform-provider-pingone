```release-note:enhancement
`resource/pingone_population`: Support for new `alternative_identifiers`, `preferred_language`, and `theme.id` attributes. Also added support for a new `password_policy.id` attribute to align with the API. This attribute should be used in place of the now-deprecated `password_policy_id` attribute.
```

```release-note:note
`resource/pingone_population`: Deprecated the `password_policy_id` attribute, which will be removed in a future major release. The new `password_policy.id` attribute should be used instead.
```

```release-note:note
bump `github.com/patrickcping/pingone-go-sdk-v2` 0.12.14 => 0.12.15
```

```release-note:note
bump `github.com/patrickcping/pingone-go-sdk-v2/management` 0.54.0 => 0.55.0
```