```release-note:enhancement
`resource/pingone_risk_policy`: Added `mitigations`, `fallback`, and `targets` attributes to support mitigation-style risk policies (mutually exclusive with `overrides`)
```
